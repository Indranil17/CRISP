name: Tests

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  schedule:
    - cron: "0 0 * * 0"

jobs:
  test:
    name: Test on ${{ matrix.os }}, Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macOS-latest, ubuntu-latest, windows-latest]
        python-version: [3.9, "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v6
      - name: Additional info about the build
        shell: bash
        run: |
          uname -a
          df -h
          ulimit -a

      - name: Setup micromamba (attempt 1)
        if: runner.os == 'Windows'
        id: setup-micromamba-windows-attempt-1
        continue-on-error: true
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: devtools/conda-envs/test_env.yaml
          environment-name: test
          init-shell: bash
          condarc: |
            channels:
              - conda-forge
          create-args: >- 
            python=${{ matrix.python-version }}
          micromamba-version: 2.8.1-1

      - name: Setup micromamba (retry)
        if: runner.os == 'Windows' && steps.setup-micromamba-windows-attempt-1.outcome == 'failure'
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: devtools/conda-envs/test_env.yaml
          environment-name: test
          init-shell: bash
          condarc: |
            channels:
              - conda-forge
          create-args: >- 
            python=${{ matrix.python-version }}
          micromamba-version: 2.8.1-1

      - name: Setup micromamba
        if: runner.os != 'Windows'
        uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: devtools/conda-envs/test_env.yaml
          environment-name: test
          init-shell: bash
          condarc: |
            channels:
              - conda-forge
          create-args: >- 
            python=${{ matrix.python-version }}
          micromamba-version: 2.8.1-1

      - name: Install package
        shell: bash -l {0}
        run: |
          python -m pip install . --no-deps
          micromamba list

      - name: Run tests
        shell: bash -l {0}
        run: |
          pytest -v --cov=CRISP --cov-report=xml --color=yes CRISP/tests/

      - name: CodeCov
        uses: codecov/codecov-action@v7
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-${{ matrix.os }}-py${{ matrix.python-version }}
